apiVersion: v1
kind: Service
metadata:
    name: mappyprojectsubscriptionservice
spec:
    type: LoadBalancer
    ports:
    - port: 8085 
    selector:
        app: mappyprojectsubscriptionservice